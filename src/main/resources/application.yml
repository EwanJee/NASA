spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/nasa
    username: nasa
    password: password
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true